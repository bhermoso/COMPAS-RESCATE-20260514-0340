# CAP-004-R1 — Cartografía del BLOQUE-004
## Cuaderno de Gobierno del Código COMPÁS

**Artefacto:** CAP-004-R1  
**Bloque:** BLOQUE-004  
**Revisión:** R1 (inicial)  
**Estado:** AUDITADO_PROVISIONAL  
**Fecha:** 2026-05-28  
**Fuente auditada:** index.html  
**Rango:** l.2664–l.2832  

---

## 1. Identificación del bloque

| Campo | Valor |
|-------|-------|
| Código de bloque | BLOQUE-004 |
| Nombre | Fase Inicial RELAS — #fase-1-contenido |
| Archivo fuente | index.html |
| Línea inicial | l.2664 |
| Línea final | l.2832 |
| Total de líneas | 169 |
| Dominio | HTML — contenido funcional de Fase 1 del ciclo RELAS: panel de navegación de subsecciones, informe de situación de salud, registro del Grupo-Motor, hoja de ruta |
| Continuación de | BLOQUE-003 (l.2453–2663, CAP-003-R1) — limite en apertura de `<main>` |
| Límite siguiente | l.2833 (`<!-- FASE 2: Perfil de salud local -->`) |

---

## 2. Rango auditado y verificación aritmética

| Campo | Valor |
|-------|-------|
| Inicio del rango | l.2664 (primer blanco interior de `<main>`, inmediatamente posterior al marcador de apertura de CAP-003 en l.2663) |
| Fin del rango | l.2832 (último blanco antes de `<!-- FASE 2: Perfil de salud local -->` en l.2833) |
| Total de líneas | 169 |
| Verificación aritmética | 4+13+30+60+56+6 = **169 ✓** |

---

## 3. Mapa de micro-rangos

### MR-061 — Blancos de apertura y delimitación de fase (l.2664–l.2667)

**Extensión:** 4 líneas  
**Tipo:** Estructural / demarcación  

| Línea | Contenido |
|-------|-----------|
| l.2664 | Blanco de apertura (primer contenido de `<main>` tras CAP-003) |
| l.2665 | `<!-- FASE 1 -->` — comentario de demarcación de fase |
| l.2666 | Blanco |
| l.2667 | `<div class="fase-contenido activa" id="fase-1-contenido">` — apertura del contenedor principal BLOQUE-004 |

**Entidades iniciadas:** ENT-041  
**Nota:** La clase `activa` en l.2667 es observable en el HTML estático — indica el estado de visibilidad inicial de la Fase 1 en carga de página. Véase CON-027.

---

### MR-062 — Panel de botones de fase (.botones-fase) (l.2668–l.2680)

**Extensión:** 13 líneas  
**Tipo:** Interacción / navegación de subsecciones  

| Línea | Contenido |
|-------|-----------|
| l.2668 | Blanco |
| l.2669 | `<div class="botones-fase">` — apertura del panel |
| l.2670 | Blanco |
| l.2671 | `<button class="btn-accion" id="btn-valoracion">Informe de la situación de salud de <span class="nombre-municipio-dinamico">Padul</span></button>` |
| l.2672 | Blanco |
| l.2673 | `<button class="btn-accion secundario" id="btn-registro">Registro del Grupo-Motor</button>` |
| l.2674 | Blanco |
| l.2675 | `<button class="btn-accion terciario" id="btn-hoja-ruta">Hoja de ruta</button>` |
| l.2676 | Blanco |
| l.2677 | `<!-- CAMBIO COMPAS: eliminar inline que anulaba CSS -->` — comentario histórico de intervención |
| l.2678 | `<button class="btn-accion btn-exportar" id="btn-exportar-json">📊 Exportar JSON</button>` |
| l.2679 | Blanco |
| l.2680 | `</div>` — cierre .botones-fase |

**Entidades cubiertas:** ENT-042  
**Notas:**
- Ninguno de los 4 botones tiene atributo `onclick` inline. El mecanismo de activación es FUERA_DEL_RANGO_AUDITADO. Véase GAP-25 y REL-030.
- `<span class="nombre-municipio-dinamico">Padul</span>` en l.2671: el texto "Padul" es un valor estático en el HTML; la clase `nombre-municipio-dinamico` indica sustitución en tiempo de ejecución vinculada al selector ENT-039 (CAP-003). El mecanismo de actualización es FUERA_DEL_RANGO_AUDITADO. Véase GAP-18 (CAP-003).
- El comentario en l.2677 documenta una intervención pasada: se eliminó un `style` inline del botón `btn-exportar-json` que anulaba reglas CSS. El botón resultante no tiene estilos inline — usa exclusivamente clases CSS.
- Las clases `secundario` (l.2673) y `terciario` (l.2675) son modificadores del patrón `btn-accion`. Sus definiciones CSS están en FUERA_DEL_RANGO_AUDITADO (CAP-001).

---

### MR-063 — Contenedor del informe de situación de salud (#informe-container) (l.2681–l.2710)

**Extensión:** 30 líneas  
**Tipo:** Contenedor funcional — visualización y exportación del informe de salud  

| Línea | Contenido |
|-------|-----------|
| l.2681 | Blanco |
| l.2682 | `<div class="informe-container" id="informe-container">` |
| l.2683 | Blanco |
| l.2684 | `<div class="export-buttons export-top" style="margin-bottom:1rem; padding:0.75rem 1rem; background:#f8fafc; border-radius:8px; border:1px solid #e2e8f0; display:flex; align-items:center; flex-wrap:wrap; gap:0.5rem;">` |
| l.2685 | Blanco |
| l.2686 | `<span style="color:#475569; font-size:0.9rem; font-weight:600;">📥 Exportar:</span>` |
| l.2687 | Blanco |
| l.2688 | `<button onclick="exportarInformeWord()" class="btn-export"><span>📄</span> Word</button>` |
| l.2689 | Blanco |
| l.2690 | `<button onclick="exportarInformePDF()" class="btn-export"><span>📑</span> PDF</button>` |
| l.2691 | Blanco |
| l.2692 | `<button onclick="imprimirInforme()" class="btn-export"><span>🖨️</span> Imprimir</button>` |
| l.2693 | Blanco |
| l.2694 | `</div>` — cierre .export-buttons.export-top |
| l.2695 | Blanco |
| l.2696 | `<div id="contenido-informe-dinamico"></div>` — contenedor de contenido dinámico (vacío en HTML estático) |
| l.2697 | Blanco |
| l.2698 | `<div class="export-buttons export-bottom" style="margin-top:1.5rem; padding:0.75rem 1rem; background:#f8fafc; border-radius:8px; border:1px solid #e2e8f0; display:flex; align-items:center; flex-wrap:wrap; gap:0.5rem;">` |
| l.2699 | Blanco |
| l.2700 | `<span style="color:#475569; font-size:0.9rem; font-weight:600;">📥 Exportar:</span>` |
| l.2701 | Blanco |
| l.2702 | `<button onclick="exportarInformeWord()" class="btn-export"><span>📄</span> Word</button>` |
| l.2703 | Blanco |
| l.2704 | `<button onclick="exportarInformePDF()" class="btn-export"><span>📑</span> PDF</button>` |
| l.2705 | Blanco |
| l.2706 | `<button onclick="imprimirInforme()" class="btn-export"><span>🖨️</span> Imprimir</button>` |
| l.2707 | Blanco |
| l.2708 | `</div>` — cierre .export-buttons.export-bottom |
| l.2709 | Blanco |
| l.2710 | `</div>` — cierre #informe-container |

**Entidades cubiertas:** ENT-043, ENT-044  
**Notas:**
- `.export-buttons.export-top` (l.2684) y `.export-buttons.export-bottom` (l.2698) son estructuralmente idénticas: mismos 3 botones, mismos `onclick`, mismos textos y clases. Única diferencia observable: `margin-bottom:1rem` vs `margin-top:1.5rem` en los estilos inline de sus contenedores. Véase DV-020.
- Ambos contenedores .export-buttons usan estilos inline completos de layout (l.2684, l.2698) en lugar de clases CSS exclusivas. Véase DV-021.
- `#contenido-informe-dinamico` (l.2696) está vacío en el HTML estático: no contiene nodo hijo alguno. Es un contenedor placeholder cuyo contenido es generado en tiempo de ejecución. Véase GAP-23.
- Las funciones `exportarInformeWord()`, `exportarInformePDF()`, `imprimirInforme()` aparecen dos veces cada una — las duplicaciones de `onclick` son coherentes con la duplicación estructural. Véase REL-024.

---

### MR-064 — Registro del Grupo-Motor (#registro-container) (l.2711–l.2770)

**Extensión:** 60 líneas  
**Tipo:** Contenedor funcional — formulario de alta y listado de miembros  

| Línea | Contenido |
|-------|-----------|
| l.2711 | Blanco |
| l.2712 | `<div class="registro-container" id="registro-container">` |
| l.2713 | Blanco |
| l.2714 | `<div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem;">` — cabecera inline-flex |
| l.2715 | Blanco |
| l.2716 | `<h3>📋 Registro de miembros del Grupo-Motor</h3>` |
| l.2717 | Blanco |
| l.2718 | `<div class="export-buttons">` |
| l.2719 | Blanco |
| l.2720 | `<button onclick="exportarMiembrosWord()" class="btn-export"><span>📄</span> Word</button>` |
| l.2721 | Blanco |
| l.2722 | `<button onclick="exportarMiembrosPDF()" class="btn-export"><span>📑</span> PDF</button>` |
| l.2723 | Blanco |
| l.2724 | `<button onclick="imprimirMiembros()" class="btn-export"><span>🖨️</span> Imprimir</button>` |
| l.2725 | Blanco |
| l.2726 | `</div>` — cierre .export-buttons de cabecera |
| l.2727 | Blanco |
| l.2728 | `</div>` — cierre cabecera inline-flex |
| l.2729 | Blanco |
| l.2730 | `<div class="form-row">` — primera fila del formulario |
| l.2731 | Blanco |
| l.2732 | `<div class="form-group"><label>Nombre *</label><input type="text" id="nombre" required></div>` |
| l.2733 | Blanco |
| l.2734 | `<div class="form-group"><label>Teléfono</label><input type="tel" id="telefono"></div>` |
| l.2735 | Blanco |
| l.2736 | `<div class="form-group"><label>Email *</label><input type="email" id="email" required></div>` |
| l.2737 | Blanco |
| l.2738 | `</div>` — cierre primera .form-row |
| l.2739 | Blanco |
| l.2740 | `<div class="form-row">` — segunda fila del formulario |
| l.2741 | Blanco |
| l.2742 | `<div class="form-group"><label>Organización</label><input type="text" id="organizacion"></div>` |
| l.2743 | Blanco |
| l.2744 | `<div class="form-group"><label>Cargo</label><input type="text" id="cargo"></div>` |
| l.2745 | Blanco |
| l.2746 | `<div class="form-group"><label>Sector *</label>` |
| l.2747 | Blanco |
| l.2748 | `<select id="sector" required>` |
| l.2749 | Blanco |
| l.2750 | `<option value="">Seleccionar...</option>` |
| l.2751 | Blanco |
| l.2752 | `<option value="politico-institucional">Político-institucional</option>` |
| l.2753 | Blanco |
| l.2754 | `<option value="salud-sspa">Salud (SSPA)</option>` |
| l.2755 | Blanco |
| l.2756 | `<option value="tecnico-aapp">Técnico/a AAPP</option>` |
| l.2757 | Blanco |
| l.2758 | `<option value="tejido-asociativo">Tejido asociativo</option>` |
| l.2759 | Blanco |
| l.2760 | `</select>` — cierre #sector |
| l.2761 | Blanco |
| l.2762 | `</div>` — cierre .form-group de Sector |
| l.2763 | Blanco |
| l.2764 | `</div>` — cierre segunda .form-row |
| l.2765 | Blanco |
| l.2766 | `<button class="btn-registrar" onclick="registrarMiembro()">✅ Registrar</button>` |
| l.2767 | Blanco |
| l.2768 | `<div class="miembros-lista"><h4>👥 Miembros</h4><div id="tabla-miembros"><p class="sin-miembros">No hay miembros.</p></div></div>` |
| l.2769 | Blanco |
| l.2770 | `</div>` — cierre #registro-container |

**Entidades cubiertas:** ENT-045, ENT-046  
**Notas:**
- Formulario de 6 campos distribuidos en 2 filas:
  - Fila 1: `#nombre` (text, required), `#telefono` (tel, opcional), `#email` (email, required)
  - Fila 2: `#organizacion` (text, opcional), `#cargo` (text, opcional), `#sector` (select, required)
- `#sector` (l.2748–l.2760) tiene 5 opciones: placeholder vacío (`value=""`, "Seleccionar...") más 4 valores operacionales: `politico-institucional`, `salud-sspa`, `tecnico-aapp`, `tejido-asociativo`. El campo es `required`, por lo que el placeholder vacío no puede enviarse como valor válido.
- `#tabla-miembros` (l.2768) muestra el estado inicial `<p class="sin-miembros">No hay miembros.</p>` — contenido placeholder estático. Su reemplazo dinámico por la lista real de miembros es FUERA_DEL_RANGO_AUDITADO. Véase GAP-22.
- La cabecera del container (l.2714) usa `style` inline para el layout flex — mismo patrón que DV-021.
- Los IDs de los campos del formulario (`#nombre`, `#email`, `#sector`, etc.) son presumiblemente consumidos directamente por `registrarMiembro()` (REL-026) — su modificación o renombrado es un riesgo de modificación de alta severidad.

---

### MR-065 — Hoja de ruta (#hoja-ruta-container) (l.2771–l.2826)

**Extensión:** 56 líneas  
**Tipo:** Contenedor funcional — gestión de hitos del proceso  

| Línea | Contenido |
|-------|-----------|
| l.2771 | Blanco |
| l.2772 | `<div class="hoja-ruta-container" id="hoja-ruta-container">` |
| l.2773 | Blanco |
| l.2774 | `<div class="hoja-ruta">` |
| l.2775 | Blanco |
| l.2776 | `<div class="hoja-ruta-header">` |
| l.2777 | Blanco |
| l.2778 | `<div><h3>🗺️ Hoja de ruta</h3><p class="hoja-ruta-intro">Gestione los hitos del proceso.</p></div>` |
| l.2779 | Blanco |
| l.2780 | `<div class="hoja-ruta-acciones">` |
| l.2781 | Blanco |
| l.2782 | `<button onclick="exportarHojaRutaWord()" class="btn-export" title="Exportar a Word"><span>📄</span></button>` |
| l.2783 | Blanco |
| l.2784 | `<button onclick="exportarHojaRutaPDF()" class="btn-export" title="Exportar a PDF"><span>📑</span></button>` |
| l.2785 | Blanco |
| l.2786 | `<button onclick="imprimirHojaRuta()" class="btn-export" title="Imprimir"><span>🖨️</span></button>` |
| l.2787 | Blanco |
| l.2788 | `<button class="btn-ordenar-fecha" onclick="ordenarHitosPorFecha()">📅 Ordenar</button>` |
| l.2789 | Blanco |
| l.2790 | `<button class="btn-añadir-hito" onclick="mostrarFormularioHito()">➕ Añadir</button>` |
| l.2791 | Blanco |
| l.2792 | `</div>` — cierre .hoja-ruta-acciones |
| l.2793 | Blanco |
| l.2794 | `</div>` — cierre .hoja-ruta-header |
| l.2795 | Blanco |
| l.2796 | `<div id="formulario-hito-personalizado" class="formulario-hito">` |
| l.2797 | Blanco |
| l.2798 | `<h4>✏️ Nuevo hito</h4>` |
| l.2799 | Blanco |
| l.2800 | `<div class="form-row">` |
| l.2801 | Blanco |
| l.2802 | `<div class="form-group"><label>Nombre *</label><input type="text" id="hito-custom-nombre"></div>` |
| l.2803 | Blanco |
| l.2804 | `<div class="form-group"><label>Fecha</label><input type="date" id="hito-custom-fecha"></div>` |
| l.2805 | Blanco |
| l.2806 | `</div>` — cierre .form-row |
| l.2807 | Blanco |
| l.2808 | `<div class="formulario-hito-botones">` |
| l.2809 | Blanco |
| l.2810 | `<button class="btn-guardar-hito" onclick="guardarHitoPersonalizado()">💾 Guardar</button>` |
| l.2811 | Blanco |
| l.2812 | `<button class="btn-cancelar-hito" onclick="ocultarFormularioHito()">Cancelar</button>` |
| l.2813 | Blanco |
| l.2814 | `</div>` — cierre .formulario-hito-botones |
| l.2815 | Blanco |
| l.2816 | `</div>` — cierre #formulario-hito-personalizado |
| l.2817 | Blanco |
| l.2818 | `<div class="hitos-lista" id="hitos-lista"></div>` — contenedor de lista de hitos (vacío en HTML estático) |
| l.2819 | `<div style="padding:.5rem 0;text-align:right;">` — contenedor inline del botón de guardado (sin blanco separador respecto a l.2818) |
| l.2820 | `<button onclick="COMPAS_guardarHojaRuta()" style="padding:.4rem .9rem;background:#16a34a;color:#fff;border:none;border-radius:6px;font-size:.8rem;cursor:pointer;font-weight:600;">💾 Guardar hoja de ruta</button>` |
| l.2821 | `</div>` — cierre contenedor inline de guardado |
| l.2822 | Blanco |
| l.2823 | `</div>` — cierre .hoja-ruta |
| l.2824 | Blanco |
| l.2825 | Blanco |
| l.2826 | `</div>` — cierre #hoja-ruta-container |

**Entidades cubiertas:** ENT-047, ENT-048, ENT-049  
**Notas:**
- El formulario de hito (#formulario-hito-personalizado, ENT-048) tiene 2 campos: `#hito-custom-nombre` (text, con asterisco de requerido en etiqueta) y `#hito-custom-fecha` (date, opcional). La validación de requerido del campo nombre no está implementada con el atributo HTML `required` — solo se indica visualmente con `*` en la etiqueta. La validación es presumiblemente JS (FUERA_DEL_RANGO_AUDITADO).
- `#hitos-lista` (l.2818) y el contenedor inline de guardado (l.2819–l.2821) aparecen en líneas consecutivas sin blanco separador entre ellos — discontinuidad de formato respecto al patrón general del bloque (separación por blancos).
- El botón de l.2820 (`COMPAS_guardarHojaRuta()`) es el único botón interactivo del bloque que no tiene ninguna clase CSS — usa exclusivamente estilos inline (color de fondo `#16a34a`, verde). El prefijo `COMPAS_` en el nombre de la función es exclusivo en este rango — los demás botones de exportación usan verbos sin prefijo de namespace. Véase REL-029 y DV-021.
- `#hitos-lista` (l.2818) está vacío en el HTML estático. Su población dinámica es FUERA_DEL_RANGO_AUDITADO. Véase GAP-24.
- Los botones de .hoja-ruta-acciones (l.2782–l.2790) son de dos tipos: exportación/impresión (3 botones con `class="btn-export"`) y gestión (2 botones con clases propias: `btn-ordenar-fecha`, `btn-añadir-hito`).

---

### MR-066 — Cierre de fase y blancos finales (l.2827–l.2832)

**Extensión:** 6 líneas  
**Tipo:** Estructural / cierre  

| Línea | Contenido |
|-------|-----------|
| l.2827 | Blanco |
| l.2828 | Blanco |
| l.2829 | Blanco |
| l.2830 | `    </div>` — cierre de #fase-1-contenido (ENT-041) |
| l.2831 | Blanco |
| l.2832 | Blanco (último blanco del rango; l.2833 inicia CAP-005) |

**Nota:** Los 3 blancos previos al cierre (l.2827–l.2829) representan una separación superior a la habitual entre el último contenedor hijo (#hoja-ruta-container, cierre en l.2826) y el `</div>` de la fase. Es un artefacto de formato, observable sin consecuencias semánticas.

---

## 4. Entidades documentadas

### ENT-041 — Contenedor principal Fase 1 (#fase-1-contenido)

| Campo | Valor |
|-------|-------|
| ID | ENT-041 |
| Selector | `div.fase-contenido.activa#fase-1-contenido` |
| Líneas | l.2667–l.2830 |
| MR asociado | MR-061 (apertura), MR-066 (cierre) |
| Estado | AUDITADO_PROVISIONAL |

**Atributos observables:**
- `class="fase-contenido activa"` — la clase `activa` está presente en el HTML estático; indica que Fase 1 es la fase visible en la carga inicial de la aplicación.
- `id="fase-1-contenido"` — referenciable como objetivo del mecanismo de visibilidad de fases (CON-027).

**Contenido directo (hijos inmediatos):**
- ENT-042 (.botones-fase)
- ENT-043 (#informe-container)
- ENT-045 (#registro-container)
- ENT-047 (#hoja-ruta-container)

**Dependencias:**
- CSS de `.fase-contenido` y `.activa`: FUERA_DEL_RANGO_AUDITADO (CAP-001)
- Consumidor JS del mecanismo de visibilidad (gestión de `.activa`): FUERA_DEL_RANGO_AUDITADO (véase CON-027)

---

### ENT-042 — Panel de botones de navegación de subsecciones (.botones-fase)

| Campo | Valor |
|-------|-------|
| ID | ENT-042 |
| Selector | `div.botones-fase` |
| Líneas | l.2669–l.2680 |
| MR asociado | MR-062 |
| Estado | AUDITADO_PROVISIONAL |

**Botones contenidos:**

| ID | Clase(s) | Texto observable | onclick |
|----|----------|-----------------|---------|
| `btn-valoracion` | `btn-accion` | "Informe de la situación de salud de [municipio]" | ninguno (inline) |
| `btn-registro` | `btn-accion secundario` | "Registro del Grupo-Motor" | ninguno (inline) |
| `btn-hoja-ruta` | `btn-accion terciario` | "Hoja de ruta" | ninguno (inline) |
| `btn-exportar-json` | `btn-accion btn-exportar` | "📊 Exportar JSON" | ninguno (inline) |

**Sub-elemento notable:** `<span class="nombre-municipio-dinamico">Padul</span>` dentro de `btn-valoracion` (l.2671). "Padul" es el valor estático del HTML; la clase `nombre-municipio-dinamico` indica sustitución en tiempo de ejecución por el municipio seleccionado en ENT-039 (CAP-003). Mecanismo: FUERA_DEL_RANGO_AUDITADO.

**Dependencias:**
- Mecanismo de activación de botones (presumiblemente `addEventListener` en JS): FUERA_DEL_RANGO_AUDITADO. Véase REL-030, GAP-25.
- CSS de `.btn-accion`, `.secundario`, `.terciario`, `.btn-exportar`: FUERA_DEL_RANGO_AUDITADO (CAP-001).

---

### ENT-043 — Contenedor del informe de situación de salud (#informe-container)

| Campo | Valor |
|-------|-------|
| ID | ENT-043 |
| Selector | `div.informe-container#informe-container` |
| Líneas | l.2682–l.2710 |
| MR asociado | MR-063 |
| Estado | AUDITADO_PROVISIONAL |

**Estructura interna observable:**
1. `.export-buttons.export-top` (l.2684–l.2694): barra de exportación superior con inline styles. Contiene 3 botones (`btn-export`) con onclick.
2. `#contenido-informe-dinamico` (l.2696): div vacío — placeholder de contenido dinámico. Véase ENT-044.
3. `.export-buttons.export-bottom` (l.2698–l.2708): barra de exportación inferior, estructuralmente idéntica a export-top excepto por `margin-top` vs `margin-bottom`.

**Dependencias:**
- Visibilidad controlada por JS (activación mediante ENT-042 btn-valoracion): FUERA_DEL_RANGO_AUDITADO. Véase GAP-25.
- Funciones de exportación e impresión: FUERA_DEL_RANGO_AUDITADO. Véase REL-024.

---

### ENT-044 — Contenedor dinámico del informe (#contenido-informe-dinamico)

| Campo | Valor |
|-------|-------|
| ID | ENT-044 |
| Selector | `div#contenido-informe-dinamico` |
| Líneas | l.2696 |
| MR asociado | MR-063 |
| Estado | AUDITADO_PROVISIONAL |

**Estado en HTML estático:** div vacío, sin nodos hijos. No tiene clases CSS adicionales ni atributos de dato. Su único identificador es el ID.

**Dependencias:**
- Contenido generado íntegramente en tiempo de ejecución: FUERA_DEL_RANGO_AUDITADO. Véase GAP-23.

---

### ENT-045 — Registro del Grupo-Motor (#registro-container)

| Campo | Valor |
|-------|-------|
| ID | ENT-045 |
| Selector | `div.registro-container#registro-container` |
| Líneas | l.2712–l.2770 |
| MR asociado | MR-064 |
| Estado | AUDITADO_PROVISIONAL |

**Estructura interna observable:**
1. Cabecera flex inline (l.2714–l.2728): título `<h3>` + barra de exportación (.export-buttons) con 3 botones.
2. Formulario fila 1 (.form-row, l.2730–l.2738): 3 campos — `#nombre` (text, required), `#telefono` (tel), `#email` (email, required).
3. Formulario fila 2 (.form-row, l.2740–l.2764): 3 campos — `#organizacion` (text), `#cargo` (text), `#sector` (select, required, 4 opciones operacionales).
4. Botón de envío (l.2766): `<button class="btn-registrar" onclick="registrarMiembro()">`.
5. Área de lista (.miembros-lista, l.2768): contiene ENT-046.

**Campos del formulario:**

| ID | Tipo | Requerido | Opciones |
|----|------|-----------|---------|
| `#nombre` | text | Sí | — |
| `#telefono` | tel | No | — |
| `#email` | email | Sí | — |
| `#organizacion` | text | No | — |
| `#cargo` | text | No | — |
| `#sector` | select | Sí | politico-institucional, salud-sspa, tecnico-aapp, tejido-asociativo |

**Dependencias:**
- `registrarMiembro()` consume los IDs del formulario: FUERA_DEL_RANGO_AUDITADO. Véase REL-026, GAP-22.
- Visibilidad controlada por JS (activación mediante ENT-042 btn-registro): FUERA_DEL_RANGO_AUDITADO.

---

### ENT-046 — Lista de miembros del Grupo-Motor (#tabla-miembros)

| Campo | Valor |
|-------|-------|
| ID | ENT-046 |
| Selector | `div#tabla-miembros` |
| Líneas | l.2768 (inline en .miembros-lista) |
| MR asociado | MR-064 |
| Estado | AUDITADO_PROVISIONAL |

**Estado en HTML estático:** contiene un único hijo `<p class="sin-miembros">No hay miembros.</p>` — estado vacío inicial. Antepuesto por `<h4>👥 Miembros</h4>` dentro del contenedor padre `.miembros-lista`.

**Dependencias:**
- Contenido dinámico (lista de miembros registrados): FUERA_DEL_RANGO_AUDITADO. Véase GAP-22.

---

### ENT-047 — Hoja de ruta (#hoja-ruta-container)

| Campo | Valor |
|-------|-------|
| ID | ENT-047 |
| Selector | `div.hoja-ruta-container#hoja-ruta-container` |
| Líneas | l.2772–l.2826 |
| MR asociado | MR-065 |
| Estado | AUDITADO_PROVISIONAL |

**Estructura interna observable:**
1. `.hoja-ruta` (l.2774–l.2823): contenedor interno del componente.
2. `.hoja-ruta-header` (l.2776–l.2794): cabecera con título + `.hoja-ruta-acciones` (5 botones: 3 exportación/impresión, 1 ordenar, 1 añadir).
3. `#formulario-hito-personalizado` (l.2796–l.2816): formulario de nuevo hito. Véase ENT-048.
4. `#hitos-lista` (l.2818): contenedor vacío de la lista de hitos. Véase ENT-049.
5. Contenedor inline de guardado (l.2819–l.2821): div anónimo con botón `COMPAS_guardarHojaRuta()`.

**Dependencias:**
- Visibilidad controlada por JS (activación mediante ENT-042 btn-hoja-ruta): FUERA_DEL_RANGO_AUDITADO.
- Implementaciones de funciones de gestión de hitos: FUERA_DEL_RANGO_AUDITADO. Véase REL-027, REL-028, REL-029.

---

### ENT-048 — Formulario de nuevo hito (#formulario-hito-personalizado)

| Campo | Valor |
|-------|-------|
| ID | ENT-048 |
| Selector | `div.formulario-hito#formulario-hito-personalizado` |
| Líneas | l.2796–l.2816 |
| MR asociado | MR-065 |
| Estado | AUDITADO_PROVISIONAL |

**Campos del formulario:**

| ID | Tipo | Marcado requerido (etiqueta) | Atributo `required` HTML |
|----|------|------------------------------|--------------------------|
| `#hito-custom-nombre` | text | Sí (`Nombre *`) | No |
| `#hito-custom-fecha` | date | No | No |

**Nota:** El campo `#hito-custom-nombre` muestra `*` en la etiqueta pero no declara `required` HTML — la validación de requerido, si existe, es JS (FUERA_DEL_RANGO_AUDITADO).

**Botones:**
- `<button class="btn-guardar-hito" onclick="guardarHitoPersonalizado()">💾 Guardar</button>` (l.2810)
- `<button class="btn-cancelar-hito" onclick="ocultarFormularioHito()">Cancelar</button>` (l.2812)

**Dependencias:**
- CSS de `.formulario-hito`: FUERA_DEL_RANGO_AUDITADO (probable visibilidad controlada — visible/oculto). Véase CON-028.
- `guardarHitoPersonalizado()` y `ocultarFormularioHito()`: FUERA_DEL_RANGO_AUDITADO. Véase REL-028.

---

### ENT-049 — Lista de hitos (#hitos-lista)

| Campo | Valor |
|-------|-------|
| ID | ENT-049 |
| Selector | `div.hitos-lista#hitos-lista` |
| Líneas | l.2818 |
| MR asociado | MR-065 |
| Estado | AUDITADO_PROVISIONAL |

**Estado en HTML estático:** div vacío, sin nodos hijos, sin placeholder textual (a diferencia de ENT-046, que sí tiene `<p class="sin-miembros">`).

**Dependencias:**
- Contenido dinámico (hitos del proceso): FUERA_DEL_RANGO_AUDITADO. Véase GAP-24.

---

## 5. Relaciones observables

### REL-024 — Botones de exportación del informe → funciones JS de exportación

| Campo | Valor |
|-------|-------|
| ID | REL-024 |
| Tipo | Activación inline (onclick) |
| Origen | Botones en `.export-buttons.export-top` (l.2688, l.2690, l.2692) y `.export-buttons.export-bottom` (l.2702, l.2704, l.2706) dentro de ENT-043 |
| Destino | `exportarInformeWord()`, `exportarInformePDF()`, `imprimirInforme()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |

**Nota:** Cada función aparece dos veces (una por barra). La duplicación es coherente con la estructura DV-020.

---

### REL-025 — Botones de exportación del Grupo-Motor → funciones JS de exportación

| Campo | Valor |
|-------|-------|
| ID | REL-025 |
| Tipo | Activación inline (onclick) |
| Origen | Botones en `.export-buttons` de la cabecera de ENT-045 (l.2720, l.2722, l.2724) |
| Destino | `exportarMiembrosWord()`, `exportarMiembrosPDF()`, `imprimirMiembros()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |

---

### REL-026 — Botón Registrar → registrarMiembro()

| Campo | Valor |
|-------|-------|
| ID | REL-026 |
| Tipo | Activación inline (onclick) |
| Origen | `<button class="btn-registrar" onclick="registrarMiembro()">` en l.2766 dentro de ENT-045 |
| Destino | `registrarMiembro()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |
| Consumidores inferibles | IDs del formulario ENT-045 (`#nombre`, `#telefono`, `#email`, `#organizacion`, `#cargo`, `#sector`) y ENT-046 (`#tabla-miembros`) — INFERIDO por proximidad estructural y convención de nombre; no verificable desde este rango |

---

### REL-027 — Botones de exportación de Hoja de Ruta → funciones JS de exportación

| Campo | Valor |
|-------|-------|
| ID | REL-027 |
| Tipo | Activación inline (onclick) |
| Origen | Botones en `.hoja-ruta-acciones` de ENT-047 (l.2782, l.2784, l.2786) |
| Destino | `exportarHojaRutaWord()`, `exportarHojaRutaPDF()`, `imprimirHojaRuta()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |

---

### REL-028 — Botones de gestión de hitos → funciones JS de hitos

| Campo | Valor |
|-------|-------|
| ID | REL-028 |
| Tipo | Activación inline (onclick) |
| Origen | Botones en `.hoja-ruta-acciones` y `.formulario-hito-botones` de ENT-047/ENT-048 (l.2788, l.2790, l.2810, l.2812) |
| Destino | `ordenarHitosPorFecha()`, `mostrarFormularioHito()`, `guardarHitoPersonalizado()`, `ocultarFormularioHito()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |
| Consumidores estructurales | ENT-048 (#formulario-hito-personalizado) y ENT-049 (#hitos-lista) son presumiblemente los objetivos de las funciones de mostrar/ocultar y guardar — INFERIDO por concordancia de nombres; no verificable desde este rango |

---

### REL-029 — Botón guardar hoja de ruta → COMPAS_guardarHojaRuta()

| Campo | Valor |
|-------|-------|
| ID | REL-029 |
| Tipo | Activación inline (onclick) |
| Origen | Botón anónimo (sin clase CSS) en l.2820 dentro de ENT-047 |
| Destino | `COMPAS_guardarHojaRuta()` |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Atributo `onclick` observable directamente en HTML |
| Nota | El prefijo `COMPAS_` en el nombre de la función es único en este rango — los demás botones de acción usan verbos sin prefijo de namespace. Este patrón de nombrado puede indicar que la función pertenece a un namespace o módulo específico. INFERIDO — no verificable desde este rango. |

---

### REL-030 — IDs del panel .botones-fase → consumidor JS implícito

| Campo | Valor |
|-------|-------|
| ID | REL-030 |
| Tipo | Referencia implícita (ID sin onclick) |
| Origen | `btn-valoracion`, `btn-registro`, `btn-hoja-ruta`, `btn-exportar-json` en ENT-042 |
| Destino | Mecanismo JS de activación/visibilidad de subsecciones (presumiblemente `addEventListener` o similar) |
| Estado destino | FUERA_DEL_RANGO_AUDITADO |
| Evidencia | Los 4 botones tienen IDs semánticamente consistentes con la función de navegación entre ENT-043, ENT-045, ENT-047; ausencia de `onclick` inline es OBSERVABLE. La existencia de un consumidor JS es INFERIDA por la necesidad funcional del sistema. |

---

## 6. Contratos observables

### CON-027 — Clase `.activa` en #fase-1-contenido (visibilidad inicial)

| Campo | Valor |
|-------|-------|
| ID | CON-027 |
| Elemento | ENT-041 (`div.fase-contenido.activa#fase-1-contenido`) |
| Línea | l.2667 |
| Evidencia directa | Clase `activa` presente en el HTML estático |
| Significado observable | La Fase 1 arranca en estado visible; las demás fases RELAS (ENT-040, CAP-003) carecen de `activa` en su HTML estático |
| Mecanismo CSS | FUERA_DEL_RANGO_AUDITADO (CAP-001): la regla CSS que convierte `.activa` en visibilidad no está en este rango |
| Mecanismo JS | FUERA_DEL_RANGO_AUDITADO: el consumidor que alterna `.activa` entre fases no está en este rango |
| Estado del contrato | PARCIAL — el activador (`.activa` en HTML estático) es observable; la implementación CSS+JS es FUERA_DEL_RANGO_AUDITADO |

---

### CON-028 — Clase `.formulario-hito` en #formulario-hito-personalizado (visibilidad condicional)

| Campo | Valor |
|-------|-------|
| ID | CON-028 |
| Elemento | ENT-048 (`div.formulario-hito#formulario-hito-personalizado`) |
| Línea | l.2796 |
| Evidencia directa | Clase `formulario-hito` presente; funciones `mostrarFormularioHito()` y `ocultarFormularioHito()` referenciadas en REL-028 con nombres que indican un contrato de visibilidad |
| Mecanismo CSS | FUERA_DEL_RANGO_AUDITADO: la regla CSS que gobierna el estado visible/oculto no está en este rango |
| Mecanismo JS | FUERA_DEL_RANGO_AUDITADO: implementaciones de mostrar/ocultar no visibles desde este rango |
| Estado del contrato | PARCIAL — la clase y los triggers de las funciones son observables; la implementación CSS+JS es FUERA_DEL_RANGO_AUDITADO |

---

## 7. Deudas visuales / arquitectónicas

### DV-020 — Duplicación literal de barras de exportación en #informe-container

| Campo | Valor |
|-------|-------|
| ID | DV-020 |
| Elemento | ENT-043 — `.export-buttons.export-top` (l.2684–l.2694) y `.export-buttons.export-bottom` (l.2698–l.2708) |
| Observación | Las dos barras son estructuralmente idénticas: misma clase base (`export-buttons`), mismos 3 botones, mismos textos, mismos `onclick`. La única diferencia observable es el valor del margen en el estilo inline (`margin-bottom:1rem` vs `margin-top:1.5rem`). |
| Consecuencia | Cualquier cambio en los botones, textos u `onclick` de una barra debe replicarse manualmente en la otra. El riesgo de desincronización es inherente a la duplicación. |
| Carácter | OBSERVABLE — la duplicación es un hecho del HTML; si es intencional (UX: botones arriba y abajo del contenido dinámico) o es deuda de mantenimiento no es determinable desde el HTML estático. |

---

### DV-021 — Uso recurrente de estilos inline para layout y presentación de elementos auxiliares

| Campo | Valor |
|-------|-------|
| ID | DV-021 |
| Elementos afectados | `.export-buttons.export-top` y `.export-buttons.export-bottom` (l.2684, l.2698), `<span>` de etiqueta "📥 Exportar:" (l.2686, l.2700), cabecera flex de ENT-045 (l.2714), contenedor de guardado en ENT-047 (l.2819), botón `COMPAS_guardarHojaRuta()` (l.2820) |
| Observación | El bloque contiene 5 instancias de estilos inline de layout/presentación, en contraste con el uso de clases CSS para los componentes principales. El botón de l.2820 es el caso extremo: no tiene ninguna clase CSS — toda su presentación es inline. |
| Consecuencia | Los estilos inline de l.2684, l.2698 y l.2714 definen layout con valores específicos no extraíbles a través del sistema de clases sin modificar el HTML. El botón de l.2820 no puede ser afectado por cambios en las hojas de estilo del sistema. |
| Carácter | OBSERVABLE. El comentario en l.2677 (`<!-- CAMBIO COMPAS: eliminar inline que anulaba CSS -->`) evidencia que ya se han realizado intervenciones previas relacionadas con conflictos entre inline y CSS en este mismo bloque. |

---

## 8. Gaps documentados

### GAP-22 — Consumidor JS de #tabla-miembros (ENT-046) y mecanismo de renderizado

| Campo | Valor |
|-------|-------|
| ID | GAP-22 |
| Elemento | ENT-046 (`#tabla-miembros`) |
| Observación | `#tabla-miembros` muestra `<p class="sin-miembros">No hay miembros.</p>` en HTML estático. La función que reemplaza ese estado por la lista dinámica de miembros registrados, el formato del elemento de lista y el mecanismo de persistencia/recuperación de miembros no son observables en l.2664–2832. |
| Resolución | Requiere análisis de JS en l.2833+ (o en el bloque CSS/JS de CAP-001/CAP-002 si existe inicialización temprana). |
| Bloqueo | No bloqueante para la cartografía de CAP-004. |

---

### GAP-23 — Consumidor JS de #contenido-informe-dinamico (ENT-044) y mecanismo de generación del informe

| Campo | Valor |
|-------|-------|
| ID | GAP-23 |
| Elemento | ENT-044 (`#contenido-informe-dinamico`) |
| Observación | El div está vacío en el HTML estático. El mecanismo que genera el contenido del informe de situación de salud (estructura, datos, visualizaciones) no es observable en l.2664–2832. |
| Resolución | Requiere análisis de JS en l.2833+. |
| Bloqueo | No bloqueante. |

---

### GAP-24 — Consumidor JS de #hitos-lista (ENT-049) y ciclo de vida de los hitos

| Campo | Valor |
|-------|-------|
| ID | GAP-24 |
| Elemento | ENT-049 (`#hitos-lista`) |
| Observación | El div está vacío en el HTML estático, sin siquiera un placeholder textual. El mecanismo de creación, renderizado, ordenación y guardado de hitos (referenciado en REL-028 y REL-029) no es observable en este rango. |
| Resolución | Requiere análisis de JS en l.2833+. |
| Bloqueo | No bloqueante. |

---

### GAP-25 — Consumidor JS de los IDs del panel .botones-fase (ENT-042) y mecanismo de visibilidad de subsecciones

| Campo | Valor |
|-------|-------|
| ID | GAP-25 |
| Elemento | ENT-042 — botones `btn-valoracion`, `btn-registro`, `btn-hoja-ruta`, `btn-exportar-json` |
| Observación | Los 4 botones tienen IDs pero no tienen `onclick` inline. El mecanismo JS que captura los clicks y controla qué subsección (ENT-043, ENT-045, ENT-047) es visible en cada momento no está en l.2664–2832. La conexión entre botón e ID de subsección es INFERIDA por concordancia semántica; no es verificable desde este rango. |
| Resolución | Requiere análisis de JS en l.2833+. |
| Bloqueo | No bloqueante. |

---

### GAP-26 — Implementaciones completas de funciones de exportación e impresión

| Campo | Valor |
|-------|-------|
| ID | GAP-26 |
| Elementos | REL-024 (`exportarInformeWord`, `exportarInformePDF`, `imprimirInforme`), REL-025 (`exportarMiembrosWord`, `exportarMiembrosPDF`, `imprimirMiembros`), REL-027 (`exportarHojaRutaWord`, `exportarHojaRutaPDF`, `imprimirHojaRuta`) |
| Observación | Las 9 funciones de exportación/impresión son referenciadas mediante `onclick` (OBSERVABLE) pero sus implementaciones — incluyendo las librerías empleadas, el formato generado y los datos consumidos — no están en l.2664–2832. |
| Resolución | Requiere análisis de JS en l.2833+. |
| Bloqueo | No bloqueante. |

---

### GAP-27 — Mecanismo y destino de persistencia de COMPAS_guardarHojaRuta()

| Campo | Valor |
|-------|-------|
| ID | GAP-27 |
| Elemento | REL-029 — botón de l.2820 |
| Observación | `COMPAS_guardarHojaRuta()` es la única función con prefijo de namespace `COMPAS_` en este rango. Su implementación, el mecanismo de persistencia (localStorage, Firebase, servidor, etc.) y el alcance de los datos guardados no son observables en l.2664–2832. |
| Resolución | Requiere análisis de JS en l.2833+. El prefijo `COMPAS_` puede ser relevante para localizar la función en el código JS. |
| Bloqueo | No bloqueante. |

---

## 9. Riesgos de modificación

| ID | Elemento | Descripción del riesgo | Severidad |
|----|----------|----------------------|-----------|
| RIESGO-001 | ENT-044 (#contenido-informe-dinamico) | El div está vacío y carece de atributos secundarios. Cualquier modificación de su estructura (adición de clases, wrappers, atributos) puede romper el mecanismo JS que lo usa como punto de inserción de contenido. | ALTA |
| RIESGO-002 | ENT-045 (IDs del formulario) | Los IDs `#nombre`, `#email`, `#sector`, `#telefono`, `#organizacion`, `#cargo` son presumiblemente consumidos directamente por `registrarMiembro()`. Renombrar cualquiera de estos IDs rompe la función. | ALTA |
| RIESGO-003 | ENT-042 (IDs de .botones-fase) | Los IDs `btn-valoracion`, `btn-registro`, `btn-hoja-ruta`, `btn-exportar-json` tienen un consumidor JS implícito no visible desde este rango. Modificar los IDs, añadir/eliminar botones o alterar la clase `.botones-fase` puede romper el mecanismo de navegación. | ALTA |
| RIESGO-004 | ENT-049 (#hitos-lista) y ENT-048 (#formulario-hito-personalizado) | El consumidor de `#hitos-lista` y el mecanismo de visibilidad de `#formulario-hito-personalizado` están en JS externo. Modificar estos IDs o clases (`formulario-hito`) rompe las funciones referenciadas en REL-028. | ALTA |
| RIESGO-005 | REL-029 (COMPAS_guardarHojaRuta) | El botón de guardado de la hoja de ruta (l.2820) tiene toda su presentación en estilos inline y ninguna clase CSS. Es el único punto de guardado de hoja de ruta observable. Alterar el `onclick` sin conocer la implementación completa puede afectar la persistencia de datos. | ALTA |
| RIESGO-006 | DV-020 (export-top / export-bottom) | Las dos barras de exportación de ENT-043 son duplicadas. Todo cambio funcional (nuevo botón, cambio de función, cambio de texto) debe aplicarse en ambas. La modificación de una sola barra produce inconsistencia observable. | MEDIA |
| RIESGO-007 | DV-021 (estilos inline) | Los estilos inline de l.2684, l.2698, l.2714, l.2819, l.2820 no están afectados por cambios en hojas de estilo del sistema. Intentar centralizar estos estilos requiere modificaciones HTML y una verificación de equivalencia visual. | BAJA |

---

## 10. Límites de auditabilidad

| Categoría | Detalle |
|-----------|---------|
| CSS del bloque | Todas las clases de ENT-041/049 (`.fase-contenido`, `.activa`, `.botones-fase`, `.btn-accion`, `.informe-container`, `.registro-container`, `.hoja-ruta-container`, etc.) están definidas en CAP-001 (l.1–l.1351): FUERA_DEL_RANGO_AUDITADO. |
| Implementaciones JS | Las 15 funciones referenciadas mediante `onclick` en este bloque, más el consumidor implícito de ENT-042, residen en l.2833+ o en scripts anteriores: FUERA_DEL_RANGO_AUDITADO. |
| Contenido dinámico | ENT-044, ENT-046, ENT-049 están vacíos en HTML estático. Su estado real en ejecución no es observable desde este rango. |
| Visibilidad de subsecciones | El estado visible/oculto en ejecución de ENT-043, ENT-045, ENT-047 no es determinable desde HTML estático sin las reglas CSS y la lógica JS correspondientes. |
| Prefijo COMPAS_ | El significado arquitectónico del prefijo `COMPAS_` en `COMPAS_guardarHojaRuta()` (namespace, módulo, convención) no es verificable desde este rango. |
| Validación de formularios | La lógica de validación de `registrarMiembro()` y `guardarHitoPersonalizado()` no es observable en este rango. |

---

## 11. Resumen maestro de identificadores

| Tipo | Rango asignado | Total |
|------|---------------|-------|
| MR | MR-061 → MR-066 | 6 |
| ENT | ENT-041 → ENT-049 | 9 |
| REL | REL-024 → REL-030 | 7 |
| CON | CON-027 → CON-028 | 2 |
| DV | DV-020 → DV-021 | 2 |
| GAP | GAP-22 → GAP-027 | 6 |

**Próximos identificadores disponibles:** MR-067, ENT-050, REL-031, CON-029, GAP-028, DV-022

---

## 12. Evaluación provisional

| Indicador | Estado |
|-----------|--------|
| Cobertura de rango (169 líneas) | COMPLETA — verificada aritméticamente (4+13+30+60+56+6=169) |
| Entidades identificadas | 9 — AUDITADO_PROVISIONAL |
| Relaciones documentadas | 7 — todas con evidencia observable (onclick directo o ID sin onclick) |
| Contratos documentados | 2 — ambos PARCIAL (CSS+JS fuera de rango) |
| Deudas documentadas | 2 — DV-020 (duplicación), DV-021 (inline styles) |
| Gaps documentados | 6 — todos con criterio de resolución en l.2833+ |
| Hallazgos de corrección | 0 |
| Contenido no cartografiado en rango | NINGUNO |

---

## 13. Veredicto formal

```
╔══════════════════════════════════════════════════════════════════╗
║  VEREDICTO: CONGELABLE_CON_OBSERVACIONES                        ║
╠══════════════════════════════════════════════════════════════════╣
║  CAP-004-R1 — BLOQUE-004 — l.2664–l.2832                       ║
║  Unidad: Fase Inicial RELAS — #fase-1-contenido                 ║
║  Líneas auditadas: 169 (6 MR, verificación aritmética OK)       ║
║  Entidades: 9 (ENT-041/049)                                     ║
║  Relaciones: 7 (REL-024/030)                                    ║
║  Contratos: 2 (CON-027/028, ambos PARCIAL)                      ║
║  Deudas: 2 (DV-020/021)                                         ║
║  Gaps: 6 (GAP-22/027, todos con resolución en l.2833+)          ║
║  Hallazgos mayores abiertos: 0                                  ║
╚══════════════════════════════════════════════════════════════════╝
```

**Base del veredicto:**

1. El rango l.2664–l.2832 está documentado con 100% de cobertura (169 líneas, 6 MR, verificación aritmética confirmada).
2. Las 9 entidades han sido identificadas y cartografiadas con sus atributos, IDs, clases y estructura observables.
3. Las 7 relaciones son todas fundamentadas en evidencia directa (atributo `onclick` observable o ID sin `onclick` con justificación de consumidor implícito).
4. Los 6 gaps tienen criterio de resolución explícito en l.2833+ y ninguno es bloqueante.
5. Los 2 contratos (CON-027, CON-028) son PARCIAL — sus dependencias CSS y JS están correctamente marcadas como FUERA_DEL_RANGO_AUDITADO.
6. Las 2 deudas (DV-020, DV-021) son observaciones no bloqueantes.
7. El rango no contiene ningún elemento que sea inauditables por opacidad — todos los límites de auditabilidad son atribuibles a la separación entre rango HTML y sus dependencias CSS/JS, patrón consistente con bloques anteriores.

---

*CAP-004-R1 — 2026-05-28*  
*Cuaderno de Gobierno del Código COMPÁS*
